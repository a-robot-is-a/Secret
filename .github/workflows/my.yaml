name: Test Work Flow
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Secret anzeigen
        run: echo "${{ secrets.SECRET }}"
        env:
          VITE_TEST_SECRET: ${{ secrets.SECRET }}
    
